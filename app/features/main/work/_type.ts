import type { TypeMainTabList } from '../../../lib/types/typeNavigation';
import type { BottomTabScreenProps } from '@react-navigation/bottom-tabs';

/* -----------------------------------------------
 * 画面固有のタイプ
 * ----------------------------------------------- */

export type TypeWorkScreen = BottomTabScreenProps<TypeMainTabList, 'work'>;
