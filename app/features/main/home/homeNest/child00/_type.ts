export type TypeFormValues = {
  dummyName: string;
  genres: string[];
  inquiry: string[];
  payment: string;
  theme: string;
  address: string;
  description: string;
};

export type TypeResultArea = Partial<TypeFormValues>;
